# Записка подписчикам к следующему перезапуску бота.
#
# Напишите текст ниже (под этими строками) - и при следующем запуске он
# уйдёт всем получателям первой строкой в сообщении о перезапуске.
#
# Строки, которые начинаются с решётки, - это пояснение к самому файлу,
# они НЕ отправляются. Можете их не трогать.
#
# Как отправить: написали текст -> commit -> push. Railway передеплоит,
# бот запустится и приложит записку к сообщению.
#
# ВАЖНО: записка остаётся в силе, пока вы её не сотрёте. Она уйдёт при
# КАЖДОМ перезапуске - в том числе если Railway передеплоит бота сам или
# он перезапустится после сбоя. Отправили - сотрите текст, оставьте файл
# пустым.
#
# Пример того, что сюда пишут:
#   Добавил список ФСБ, теперь следим и за террористическими организациями.
#   Бот молчал с пятницы - отвалился прокси. Ничего не пропустили,
#   всё проверил руками.

Добавил новые прокси, старые истекали
